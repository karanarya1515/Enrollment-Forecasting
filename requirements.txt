# Clinical Trial Enrollment Forecasting — Python dependencies
# Install with: pip install -r requirements.txt
#
# No database driver is required: the pipeline reads CSV extracts from disk
# and never opens a database connection.

# --- Data processing ---
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0        # parquet read/write (engine="pyarrow")

# --- Machine learning ---
xgboost>=2.0.0         # requires >=2.0 for reg:quantileerror (uncertainty.py)
scikit-learn>=1.3.0    # evaluation metrics only

# --- Dashboard ---
streamlit>=1.30.0
plotly>=5.18.0
